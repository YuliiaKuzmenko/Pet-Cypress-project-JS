// Create an “if statement” that checks if variable “first” is bigger than variable “second”:
const first = 5
const second = 4
if (first > second){
console.log('variable “first” is bigger than variable “second”');
}
